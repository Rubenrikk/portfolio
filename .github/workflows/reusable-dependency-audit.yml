name: Reusable Dependency Audit

on:
  workflow_call:
    inputs:
      node-version:
        description: Node.js version for audit jobs
        required: false
        default: '20'
        type: string
      audit-level:
        description: Minimum vulnerability level to fail on
        required: false
        default: high
        type: string

permissions:
  contents: read

jobs:
  dependency-audit:
    name: dependency-audit
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run dependency audit
        run: npm audit --audit-level="${{ inputs.audit-level }}"
